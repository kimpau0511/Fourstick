# 포스틱 코딩 규칙 (팀 4인 + AI 협업용)

> 팀원마다 AI(Claude/Codex 등)를 따로 쓰더라도 결과물이 일관되도록 만든
> 최소 규칙입니다. 무거운 프로세스가 아니라 "이거 하나는 다 같이 지키자"
> 수준으로 유지합니다. bkit(오픈소스 AI 협업 킷)의 규칙 중 스택과 무관하게
> 쓸 수 있는 부분을 가져오고, Fourstick 실제 코드에서 이미 쓰이고 있는
> 패턴을 추가했습니다.

## 1. AI에게 일 시킬 때 우선순위

- **모르면 추측하지 말고 확인한다**: 코드/이 문서에 답이 없으면 사용자에게
  묻는다. 없는 걸 지어내서 진행하지 않는다.
- **진실의 원천(SoR) 우선순위**: 실제 코드 > 이 문서/CLAUDE.md > 기획서.
  기획서와 코드가 다르면 코드가 진짜 동작이고, 기획서 쪽을 의심한다.
- **큰 변경은 확인받고, 작은 변경은 바로 한다** (아래 4번 참고). 오타
  수정에 매번 승인받을 필요는 없지만, 동작 방식이 바뀌는 결정(예: STOP을
  "한 동작만 멈춤"에서 "재개 전까지 전부 멈춤"으로 바꾼 것)은 반드시
  사람에게 설명하고 확인받는다.

## 2. 코드 품질 원칙

- **코딩 전에 검색부터**: 비슷한 기능이 이미 있는지 먼저 확인한다
  (`pipeline.py`, `safety_guard.py`, `robot_config.py`에 이미 있는 패턴을
  다시 만들지 않는다).
- **DRY**: 같은 로직이 두 번째로 필요해지는 순간 공통 함수로 뺀다.
- **SRP**: 함수 하나는 책임 하나. `process_utterance`처럼 단계별로 쪼개진
  함수 스타일을 따른다.
- **No Hardcoding**: 배포 환경마다 달라질 수 있는 값(경로, 포트, 임계값,
  토큰)은 하드코딩하지 말고 환경변수로 뺀다 — 아래 5번 참고.
- **하드코딩이 왜 문제인지**: 값을 코드에 박아두면 배포 환경이 바뀔 때마다
  소스를 고치고 다시 배포해야 한다. 환경변수로 빼면 값만 바꿔 끼우면 된다.

### 코딩 후 셀프체크
- 같은 로직이 다른 파일에도 있나?
- 이 함수, 다른 곳에서도 재사용 가능한가?
- 하드코딩된 값이 있나? (경로, 포트, 임계값, 매직넘버)
- 함수가 한 가지 일만 하나?

### 리팩터링 신호
- 같은 코드가 2번째로 등장함
- 함수가 20줄을 넘음
- if-else가 3단 이상 중첩됨
- 같은 파라미터 조합을 여러 함수에 반복해서 넘김

## 3. 안전 관련 코드 전용 원칙 (포스틱 고유)

일반 웹앱과 달리 실제 로봇을 움직이므로, 아래는 타협하지 않는다.

- **상위 레이어를 믿지 않는다**: `safety_guard.py`가 `pipeline.py`/L2 LLM
  결과를 다시 처음부터 검증하는 것처럼, 각 레이어는 이전 레이어가 이미
  검증했다고 가정하지 않고 자기 책임 범위를 독립적으로 재검증한다.
- **실행 직전 재검증**: 시간이 지나 상태가 바뀔 수 있는 모든 것(계획
  만료, 로봇 종류 일치, 해시 변조 여부)은 "만들 때"가 아니라 "실행
  직전"에 다시 확인한다(`validate_task_plan_for_execution` 패턴).
- **안전 관련 동작을 바꾸는 결정은 코드 리뷰가 아니라 사람 승인이 필요**
  (STOP 래칭 정책 변경이 그 예). "버그냐 결정이냐" 애매하면 결정으로
  취급하고 확인받는다.
- **정지(STOP)는 항상 우선 경로**: 슬롯 추출/LLM 등 일반 처리 경로보다
  먼저 분기해서 처리한다.

## 4. 작업 크기별 진행 방식

| 분류 | 예시 | AI 행동 |
|------|------|---------|
| 오타/사소한 수정 | 오타, 변수명, 주석 | 바로 실행 |
| 작은 변경 | 리팩터링, 성능 개선 | 요약만 보여주고 진행 |
| 기능 추가 | 새 엔드포인트, 새 스킬 | 설계 확인 후 진행 |
| 큰 변경 | 안전 정책 변경, 로봇 구성 변경, 아키텍처 재설계 | 반드시 설명하고 사용자 확인 후 진행 |

## 5. 네이밍 & 환경변수 규칙

### Python
- 함수/변수: `snake_case`
- 클래스: `PascalCase`
- 상수: `UPPER_SNAKE_CASE`
- 파일: `snake_case.py`
- private/모듈 내부 전용: `_leading_underscore`

### 환경변수 — `FORSTICK_` 접두사 + 대문자
이미 코드에 쓰이고 있는 패턴을 표준으로 삼습니다. 새 설정값을 추가할 때
반드시 이 규칙을 따르세요.

| 변수 | 용도 |
|------|------|
| `FORSTICK_ROBOT` | 실행할 로봇 종류 (`ur5e` 등) |
| `FORSTICK_API_TOKEN` | API 인증 토큰 (필수, 기본값 없음) |
| `FORSTICK_MODEL_ID` | LLM 모델 경로 |
| `FORSTICK_STT_MIN_CONFIDENCE` | STT 신뢰도 게이트 임계값 |
| `FORSTICK_CORS_PORTS` | CORS 허용 포트 |
| `FORSTICK_AUDIT_LOG_MAX_BYTES` / `_BACKUP_COUNT` | 감사 로그 회전 설정 |
| `FORSTICK_LOG_RETENTION_COUNT` | 이벤트 로그 개수 상한 |

새 환경변수를 추가할 때: `os.environ.get("FORSTICK_XXX", <안전한 기본값>)`
패턴을 따르고, 값이 없으면 안 되는 경우(토큰처럼)는 기본값을 두지 말고
fail-fast(`RuntimeError`로 기동 자체를 막음)로 처리합니다
(`FORSTICK_ROBOT`/`FORSTICK_API_TOKEN` 검증 코드 참고).

### 에러/거부 코드 — `<영역>-<이유>` 대문자 케밥 스타일
`A-SLOT`, `A-UNKNOWN-LOC`, `E-SEQ-001`, `E-HOLD-003`, `E-EXEC-STOPPED-LATCHED`
처럼 알파벳 접두사(A=Ask/게이트, E=Execution/실행 거부)로 영역을 구분하고,
뒤에 구체적인 이유를 붙입니다. 새 거부 사유를 추가할 때 이 패턴을 따릅니다.

## 6. 테스트

- 실제로 실행 가능한 테스트를 남긴다 — `python test_safety_guard.py`,
  `python -m unittest test_pipeline_guards` 처럼 단순 명령으로 돌아가야 함.
- 실패하면 반드시 0이 아닌 종료 코드를 반환해야 CI/사람이 잡을 수 있다
  (`sys.exit(1)` 패턴 — 예전에 이게 빠져서 실패해도 통과로 보였던 적 있음).
- 안전 관련 로직(게이트, 검증, 동시성)은 최소 하나의 재현 테스트를 남긴다.
  버그를 고쳤으면 "그 버그가 재발하면 잡히는" 테스트를 같이 추가한다.

## 7. 주석

- **무엇(what)이 아니라 왜(why)를 적는다.** 코드만 봐도 알 수 있는 설명은
  쓰지 않는다.
- 버그를 우회했거나(workaround), 직관과 다른 선택을 했다면 그 이유를 적는다
  (이 프로젝트 전체에 이미 이런 스타일의 주석이 많습니다 — 그대로 따르면 됨).
- 비영어 팀이므로 주석은 한국어로 통일합니다.

## 8. 배포 관련

- 배포 전 이 리포지토리의 `CLAUDE.md`(git에 안 올라가는 로컬 기록)를 먼저
  읽고, 마지막 세션에서 뭘 바꿨는지/뭘 남겨뒀는지 확인합니다.
- 새로운 로봇을 추가할 땐 `gazebo_robot/robot_config.py`에 `RobotConfig`
  하나만 추가하면 되도록 구조가 이미 잡혀 있습니다 — 다른 코드를 고치지
  않아도 되게 설계된 확장 포인트이니 이 패턴을 깨지 않습니다.

---
이 문서 자체도 코드처럼 관리합니다. 규칙이 실제 코드와 안 맞게 되면(코드가
바뀌었는데 문서를 안 고쳤다면) 문서를 고치는 PR을 따로 올리세요.
