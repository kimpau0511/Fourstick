"""계획 생성 계층 (md/개발플랜.md 3-03, 5단계).

책임을 셋으로 나눈다.
- `slot_extractor` — 발화에서 카탈로그에 있는 리소스를 찾는다. **모델을 쓰지
  않는다.** 결정적이고, 네트워크를 타지 않는다.
- `plan_provider` — LLM 호출 경계. 입력·출력·오류를 계약으로 고정한다.
  구현체(vLLM·Mock)가 이 뒤에 들어온다.
- `pipeline` — 둘을 잇는 얇은 조율자. 자기 안에 추출 규칙도, 모델 호출 코드도
  두지 않는다.
- `prompt` — 버전이 붙은 프롬프트·출력 스키마 렌더링. 전송은 하지 않는다.
- `output_parser` — 모델 출력을 파싱·버전 대조·구조 검증해 초안으로 만든다.
- `attempt_runner` — 정책(재시도·제한시간) 안에서 반복 실행하고 시도 기록을
  만든다.
- `redaction` — 기록에 남기기 전 비밀정보를 지운다.
- `mock_provider` — 계약 테스트 전용 Provider. 실제 LLM 검증을 대신하지 않는다.

이렇게 나누는 이유는 3-03의 목적 그대로다. 한 함수가 발화를 파싱하면서 모델을
호출하면, 모델 없이 추출을 시험할 수 없고 추출을 고칠 때 모델 경로가 함께
흔들린다.
"""
