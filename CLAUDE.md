# 포스틱 (Forstick) — Task Plan 로봇 파이프라인

음성/텍스트 지시 → Task Plan JSON 생성 → 실제 Gazebo 물리 시뮬레이션 로봇 실행까지 되는 풀스택 데모. 로봇은 Panda/UR5e+Robotiq 중 골라서 띄울 수 있음(`start_robot.sh` 또는 웹 콘솔 버튼, 아래 "로봇 전환" 참고) — 단 UR5e는 그리퍼 컨트롤러 버그로 아직 pick까지는 안 됨.
지금까지 Cowork(클라우드 채팅 세션, 이 컴퓨터에 직접 접근 불가)에서 작업하며 heredoc으로 파일을 손으로 저장해왔음.
Claude Code는 이 컴퓨터에서 직접 돌아가니, 파일을 직접 읽고 고치고 명령어도 직접 실행하면 됨 — 더 이상 heredoc 복붙 필요 없음.

## 아키텍처 (5계층)

계획을 세우는 권한(LLM)과 실행을 허가하는 권한(결정론적 규칙/기하 검사)을 분리한 설계.

- **L1** (`pipeline.py`의 `extract_slots`): 음성/텍스트 → 슬롯 추출. 정규식이 아니라 Capability Profile(위치 4개, 물건 3개)의 리터럴 문자열을 문장에서 직접 찾고 조사로 출발/도착 역할 판단. STT는 `faster-whisper`(small, CPU) + `initial_prompt`로 도메인 어휘 힌트.
- **L2** (`pipeline.py`의 `process_utterance` 내부): EXAONE-3.5-7.8B-AWQ를 vLLM으로 서빙, `response_format: json_schema`로 구조화 출력 (guided_json은 vLLM 0.28.0에서 무시됨 — 반드시 json_schema 써야 함). 5개 원자 스킬만 허용: home/move/pick/place/stop.
- **L3** (`safety_guard.py`): 8개 결정론적 안전 규칙 (E-SEQ-001~004, E-HOLD-001~002, E-LIMIT-001, E-ARG-001). ⚠️ 기획서 원본 7개 규칙과 아직 대조 안 됨, 추정 세트임.
- **실행 계층** (`gazebo_robot/taskplan_bridge.py` + `gazebo_robot/robot_config.py`): Task Plan JSON → 실제 Gazebo 로봇 실행. `/compute_ik` + `FollowJointTrajectory`(팔) + `ParallelGripperCommand`(그리퍼). 로봇마다 다른 값(관절 이름/그리퍼 파라미터/컨트롤러 액션 이름/IK 그룹/좌표)은 `robot_config.RobotConfig` 어댑터로 분리돼 있고, `TaskPlanExecutor`는 로봇-무관. 현재 `panda`/`ur5e`(UR5e+Robotiq 2F-85) 둘 다 `ROBOT_CONFIGS`에 등록됨. `taskplan_bridge.py` CLI는 `--robot {panda,ur5e}`로 선택.
  - **pick grasp 검증 + 재시도** (2026-09-08 추가): 그리퍼/팔 어디에도 effort(토크) 인터페이스가 없어서(`*.ros2_control.xacro` 참고) 관절 쪽에서는 무게를 전혀 못 느낀다 — 그래서 컨트롤러 액션이 `error_code=0`으로 끝나도 실제로 들어올렸다는 보장이 없었다(아래 "물체 위치를 직접 확인해야 함" 항목 참고). `exec_pick`이 이제 매 시도마다 hover 복귀 직후 `gz topic -e -t /world/pallets_world/pose/info -n 1 --json-output`을 서브프로세스로 호출해 물체(`OBJECT_MODEL_MAP`으로 Task Plan 물체명 → `material_a/b/c` 매핑)의 실제 world z를 읽고, 안착 높이(`grasp_z`)보다 `GRASP_LIFT_THRESHOLD_M`(3cm) 이상 안 떠 있으면 "무게 0(안 들림)"으로 판정해 그리퍼를 열고 최대 3회(`exec_pick(args, max_attempts=3)`)까지 다시 내려가서 집기를 재시도한다. 3회 다 실패하면 `pick` 스텝 자체가 실패로 끝나서 `run_plan`이 중단된다(이전엔 이 경우도 `{"success":true}`로 잘못 보고되며 다음 스텝으로 진행했음). **주의**: 이 검증은 마찰-그립 문제(아래 참고) 자체를 고치는 게 아니라 "안 들렸는데 성공했다고 거짓 보고하는" 문제만 없앤 것 — 이 환경에서 마찰만으로 못 붙잡는 상황이면 3번 재시도해도 여전히 pick 실패로 끝날 수 있다(2026-09-08 `A자재`/1번 팔레트로 실측: 3회 전부 z=0.1175로 안착 높이 그대로, pick 실패 확인됨). `gz topic` 조회 자체가 실패하면(CLI 없음/타임아웃 등) 판정 불가로 보고 기존처럼 성공 취급한다(무한 재시도 방지). `gz topic -n 1`이 타이밍에 따라 메시지를 2개 이어붙여 찍어서 `json.loads`가 "Extra data"로 죽는 버그가 있었음(실측) — `json.JSONDecoder().raw_decode()`로 앞쪽 값 하나만 파싱하도록 고침.
  - **pick 물체 위치 vision 추정** (`gazebo_robot/vision_locator.py`, 2026-09-08 추가): 지금까지 로봇은 물체 위치를 전혀 인식 못 하고 `location_poses`에 박아둔 좌표만 믿는 완전 오픈루프였음(물체가 조금이라도 밀려나 있으면 그냥 허공을 집음 — 실제로 위 grasp 재시도 로직 테스트 도중 안 건드린 `material_b`가 2번 팔레트(0.5, 0.0)에서 1번 팔레트 근처(0.515, 0.288)까지 밀려나 있는 걸 발견함, 원인 미상). `scene_camera`(`/scene_camera/image`, rgb8)를 `TaskPlanExecutor`가 직접 구독해서(`_on_camera_image`) 최신 프레임을 들고 있다가, `exec_pick`이 팔을 움직이기 **전**(그리퍼가 카메라 시야를 가리기 전)에 `vision_locator.locate_object()`로 물체 색상 블롭(`OBJECT_COLORS`: A=빨강/B=초록/C=파랑, `pallets_world.sdf`의 `<material>` 값 그대로) 중심을 찾아 world (x, y)를 추정하고, 하드코딩 좌표 대신 그 위치로 접근·하강한다. 카메라 스펙(`horizontal_fov=1.2`, 960x540, pose 고정값)으로 핀홀 카메라 모델을 세우고, 물체가 정육면체라 카메라가 실제로 보는 건 물체 중심이 아니라 윗면(`grasp_z + OBJECT_HALF_HEIGHT_M`)이라는 걸 반영해서 광선-평면 교차를 풂 — 이 보정 전엔 실측 오차 3.1~3.4cm였는데 보정 후 1~4mm로 줄어듦(2026-09-08 `gz topic`으로 읽은 실제 좌표 대조 검증, `material_a`/`material_b`로 확인). 실제 pick 실행에서도 매 시도(최초 + 재시도)마다 하드코딩(0.400,0.300) → 실측(0.399~0.402, 0.301~0.303) 보정이 일관되게 들어가는 것 확인함. **한계**: 카메라가 고정된 1대뿐이라 팔이 물체 근처로 내려가면 그리퍼 자체가 시야를 가려서(실측: 2번 팔레트로 접근한 뒤엔 `material_b`를 못 찾음) vision이 실패할 수 있음 — 이 경우 조용히 하드코딩 좌표(또는 재시도 중이면 직전에 성공했던 vision 좌표)로 폴백하고 로그만 남김(무게 검증과 마찬가지로 "판정 불가 시 안전하게 폴백" 원칙). 색상 매핑 없는 물체나 카메라 프레임이 아직 안 온 경우도 동일하게 폴백.
- **API 서버** (`api_server.py`): FastAPI, `/v1/task-plan`(텍스트), `/v1/task-plan/audio`(음성), `/v1/execute`(이미 만들어둔 task_plan을 재생성 없이 그대로 실행 — plan_id/hash 유지), `/v1/audit-log`(audit_log.jsonl을 최신순 JSON으로), `/v1/robot/switch`(로봇 전환, 아래 참고), `/health`(로봇 연결 여부 + `robot_type`/`robot_label`), `/gazebo-stream.mjpeg`(Gazebo 카메라 라이브뷰), `/`(demo.html), `/console`(forstick_ex.html). 포트 8090. 실행할 로봇은 `FORSTICK_ROBOT` 환경변수(기본값 `panda`, `ROBOT_CONFIGS` 키 중 하나)로 서버 기동 시점에 고정 — Panda/UR5e 런치 파일이 각자 독립된 Gazebo 서버 + 네임스페이스 없는 동일 이름 노드(`move_group` 등)를 띄우기 때문에 한 번에 하나의 로봇 스택만 가동 가능. 콘솔 UI(`forstick_ex.html`)는 `/health`의 `robot_type`/`robot_label`을 읽어서 로봇 이름을 동적으로 표시함.
- **로봇 전환** (`start_robot.sh` + `/v1/robot/switch`): `./start_robot.sh {panda|ur5e}`를 터미널에서 직접 실행하면 지금 떠 있는 로봇 스택(API 서버 + Gazebo+MoveIt, vLLM은 안 건드림)을 내리고 선택한 로봇으로 다시 띄운다 — PID 파일(`.run/`)로 정지 시도하고, 그래도 남는 고아 프로세스(특히 `gz-sim-main`/`gz-sim-gui-client` — `ros2 launch` 부모를 죽여도 안 죽고 남아서 다음 로봇의 Gazebo가 "Another world of the same name is running"으로 죽는 문제가 실제 있었음)를 이름으로 한 번 더 SIGTERM→SIGKILL 정리한다. 콘솔 UI(`forstick_ex.html`)의 "Panda로 전환"/"UR5e로 전환" 버튼은 `/v1/robot/switch`(POST `{"robot": "panda"|"ur5e"}`)를 호출하는데, 이 엔드포인트는 `start_robot.sh`를 `start_new_session=True`로 백그라운드 실행만 시키고 바로 응답한다 — 스크립트가 이 요청을 처리 중인 API 서버 프로세스 자체를 곧 죽이므로, 죽어도 스크립트는 살아남게 하는 게 핵심. 프론트는 응답을 받은 뒤 `/health`가 목표 로봇을 보고할 때까지 3초 간격으로 폴링(그 사이 연결 실패는 정상 — 서버가 재기동 중).
- **데모 페이지** (`demo.html`): 텍스트/마이크 입력, Task Plan 결과, Gazebo 라이브 뷰. 같은 origin(8090)에서 서빙해야 마이크(getUserMedia)가 동작함.
- **콘솔 UI** (`forstick_ex.html`, `/console` 경로): 작업 콘솔/Task Plan/안전 검증/판정 결과/시뮬레이션/감사 로그 6화면짜리 풀 UI. 원래 정적 목업이었던 걸 실제 백엔드에 연동함 — 검증 파이프라인은 실제로 존재하는 2단계(형식검사+L3 Safety Guard)+실행(`/v1/execute`)만 표시함(목업에 있던 모션/충돌 사전검사, 실행계약 토큰은 실제로 없는 기능이라 뺐음). "연결된 로봇" 카드는 `/health`의 `robot_type`/`robot_label`을 읽어 현재 로봇 이름을 동적으로 보여주고, "Panda로 전환"/"UR5e로 전환" 버튼으로 `/v1/robot/switch` 호출해 실제 로봇을 바꿀 수 있음(아래 "로봇 전환" 참고). `demo.html`과 마찬가지로 마이크 쓰려면 반드시 `http://localhost:8090/console`로 접속해야 함. curl로 `/v1/audit-log`·`/v1/execute` 엔드포인트 단위 검증은 했지만, vLLM+Gazebo 풀스택을 같이 띄운 브라우저 환경에서 전체 플로우(명령→계획→검증→실행→감사로그) 실사용 테스트는 아직 안 함 — 다음에 풀스택 띄운 김에 한 번 확인 필요.

## 실행 방법

**로봇만 바꾸고 싶을 때(vLLM은 이미 떠 있는 상태)**: `~/forstick`에서 `./start_robot.sh panda` 또는 `./start_robot.sh ur5e` 한 줄이면 Gazebo+MoveIt/API 서버까지 알아서 내렸다 다시 띄워줌. 웹 콘솔(`/console`)의 로봇 전환 버튼도 내부적으로 같은 스크립트를 씀. 처음부터 풀스택을 띄울 때(vLLM까지)는 아래 수동 절차 참고.

**Panda+UR5e를 동시에 띄우고 싶을 때**: `./start_dual_robots.sh` — 노드 이름(`move_group` 등)과 서비스 이름(`/compute_ik` 등)이 두 로봇 다 동일해서 그냥 같이 띄우면 충돌하는데, 노드를 리네임하는 대신 통신 계층을 통째로 격리해서 해결함 — `ROS_DOMAIN_ID`(Panda=0, UR5e=1, Cyclone DDS가 도메인 다르면 discover 자체를 안 함)와 `GZ_PARTITION`(Panda=`forstick_panda`, UR5e=`forstick_ur5e`, Gazebo Transport 쪽의 같은 개념 — 같은 `pallets_world.sdf`를 그대로 써도 "Another world of the same name is running" 없이 둘 다 뜸). 로봇별 코드(`robot_config.py`/`taskplan_bridge.py`/`ur5e_robotiq_gazebo.launch.py`) 수정 없이 스크립트만으로 격리됨 — `panda_gazebo_moveit.launch.py`에는 UR5e처럼 헤드리스 옵션(`gazebo_gui` launch arg)만 추가함. Panda는 `localhost:8090`, UR5e는 `localhost:8091`에 각자 독립된 API 서버+콘솔(`/console`)이 뜸 — 통합 UI는 아니라 화면 자체는 로봇별로 따로지만, 콘솔의 "Panda로 전환"/"UR5e로 전환" 버튼이 듀얼 모드일 땐 이걸 인지해서 다른 포트가 이미 떠 있으면(짧게 `/health` 확인) 재기동 없이 그 포트의 `/console`로 바로 이동만 함(즉시 전환) — 단일 모드일 때만 기존 `/v1/robot/switch`(재기동, 2~3분) 흐름을 씀. 이걸 가능하게 하려고 `api_server.py`에 CORS(포트 8090/8091 origin 허용, GET만)를 추가함 — 브라우저가 다른 포트로 fetch할 수 있어야 이 판별이 되기 때문. 기본은 둘 다 GUI로 띄움(로봇별로 화면을 따로 보고 싶다는 요청 때문 — Gazebo 창이 로봇당 하나씩 따로 뜸, WSLg 부하가 부담되면 `start_dual_robots.sh`의 `GAZEBO_GUI` 배열을 헤드리스로 바꿔도 됨). `./stop_dual_robots.sh`로 둘 다 내림. `start_robot.sh`(하나만 전환하는 기존 흐름)와는 별개 워크플로우로 공존 — 서로의 PID 파일이 안 겹치지만, 듀얼 모드를 켜면 포트 8090 충돌 방지를 위해 단일 모드 스택은 자동으로 먼저 내려감. **주의**: 처음 이 스크립트를 만들 때 `GZ_PARTITION`을 담는 bash 배열 이름을 실수로 `GZ_PARTITION`(export하려는 환경변수와 동일)으로 지어서 격리가 통째로 안 먹힌 적이 있었음(로봇 두 대가 한 Gazebo 창에 같이 뜨고, `gz_ros2_control`이 세그폴트로 죽는 증상 — 원인/수정 내용은 `start_dual_robots.sh` 상단 주석 참고, 지금은 배열 이름을 `GZ_PART`로 바꿔 고쳐짐). 비슷한 실수 재발 방지 차원에서, 기동 후 `pgrep -af gz-sim-main`으로 프로세스가 로봇 수만큼(2개) 떠 있는지, 각 프로세스 커맨드라인이 실제로 다른지 확인하는 습관을 들일 것.

각 터미널마다 필요:
```bash
source /opt/ros/lyrical/setup.bash
export RMW_IMPLEMENTATION=rmw_cyclonedds_cpp   # Fast-DDS가 이 배포판에서 불안정, Cyclone DDS로 고정
```

1. vLLM (`~/forstick/venv`에서):
```bash
VLLM_USE_V2_MODEL_RUNNER=0 python3 -m vllm.entrypoints.openai.api_server \
  --model /home/asd/models/exaone-3.5-7.8b-awq --quantization awq \
  --gpu-memory-utilization 0.85 --max-model-len 4096 --enforce-eager --trust-remote-code
```
2. Gazebo + MoveIt: `ros2 launch gazebo_robot panda_gazebo_moveit.launch.py` (또는 해당 launch 파일 직접 실행)
3. API 서버 (`~/forstick/venv`에서, **`--reload` 쓰지 말 것** — rclpy 노드가 파일 변경마다 재시작되면 문제됨):
```bash
uvicorn api_server:app --host 0.0.0.0 --port 8090
```
4. 브라우저에서 `http://localhost:8090/`

## 환경 특이사항

- WSL2 Ubuntu 26.04 "Resolute", ROS2 "Lyrical Luth", Gazebo "Jetty" — 전부 매우 최신 배포판이라 버전 고유 버그를 이 프로젝트에서 여러 번 만남 (아래 참고).
- venv 3개 있음: `~/forstick/venv`(vllm + fastapi + rclpy 전부 있음, API 서버/vLLM 둘 다 이걸로), `~/venv_ros`, `~/venv`(vllm 없음). **API 서버는 반드시 `~/forstick/venv`에서 실행.**
- 파일 저장 시 **꼭 터미널에 직접 붙여넣거나 편집기로 정상 저장** — 이 프로젝트에서 heredoc 텍스트를 nano에 잘못 붙여넣어서 파일이 깨진 사고가 두 번 있었음 (셸 명령 자체가 파일 내용으로 저장됨). Claude Code는 파일을 직접 쓰니 이 문제 자체가 없음.
- 다른 컴퓨터에서 이 데모(특히 마이크)에 접속하려면: LAN IP는 브라우저가 "보안 컨텍스트"로 안 봐서 마이크(getUserMedia)가 막힘. `localhost`/`127.0.0.1`만 기본 허용. LAN에서 마이크까지 쓰려면 각 클라이언트 Chrome에서 `chrome://flags/#unsafely-treat-insecure-origin-as-secure`에 주소 등록하거나, 서버에 자체서명 HTTPS 인증서 필요.

### 이 배포판에서 만난 버전 고유 버그들 (재발하면 참고)

- `move_group` C++ 노드가 libfastdds.so에서 SIGSEGV → RMW를 Cyclone DDS로 전환해서 해결 (`ros-lyrical-rmw-cyclonedds-cpp`).
- `gz_ros2_control` 플러그인이 조용히 로드 실패 → `GZ_SIM_SYSTEM_PLUGIN_PATH=/opt/ros/lyrical/lib` 필요 (패키징 누락으로 보임).
- `moveit_py`/MoveItCpp가 "Failed to load any planning pipelines"로 항상 실패 → MoveItPy 안 쓰고 `FollowJointTrajectory` 액션을 직접 호출하는 방식으로 우회 (`taskplan_bridge.py`가 이 방식).
- `static_transform_publisher`가 이 릴리스에서 positional args 제거됨 → `--frame-id`/`--child-frame-id` 플래그 사용.
- `controller_manager`가 spawner 자식 프로세스에 `--params-file`을 안 넘겨줌 → 각 컨트롤러 spawner Node에 `-p <yaml경로>` 직접 지정해서 우회.
- 그리퍼 컨트롤러가 `GripperCommand`가 아니라 `ParallelGripperCommand` 액션 타입 사용 (goal이 `sensor_msgs/JointState` 형태).
- RViz2가 WSLg에서 GLX 크래시 → `LIBGL_ALWAYS_SOFTWARE=1` + `QT_QPA_PLATFORM=xcb` 둘 다 필요.
- WSLg 화면은 x11grab으로 캡처 불가(항상 검은 화면) → Gazebo scene_camera 센서를 ROS2 토픽으로 직접 구독해서 스트리밍하는 방식으로 우회.
- `/health`는 taskplan_bridge 노드가 처음에 연결만 됐으면 계속 `robot_connected: true`를 응답함 — 그 뒤 실제 Gazebo 프로세스가 죽어도 못 잡음. 로봇이 실제로 안 움직이면 `pgrep -af gz-sim-main`으로 프로세스가 살아있는지 먼저 확인할 것(듀얼 로봇 동시 기동 스크립트 만들 때 이걸로 한참 헤맸음 — 알고보니 원인은 Gazebo 버그가 아니라 `start_dual_robots.sh`의 bash 변수 이름 충돌이었음, 위 "Panda+UR5e를 동시에 띄우고 싶을 때" 참고).
- (2026-09-04 재확인, 재현 안 됨) UR5e+Robotiq의 `gripper_controller` "goal accept 후 feedback/result 무응답" 버그로 기록돼 있었는데, `ros2 action send_goal /gripper_controller/gripper_cmd`로 열기/닫기/중간값/현재위치-동일값 4가지 다 재테스트해보니 전부 정상적으로 `reached_goal: true` 받으며 성공함(소스도 확인함 — `parallel_gripper_action_controller_impl.hpp`의 `update()`가 position/velocity state interface를 못 읽으면 `check_for_success()` 자체를 호출 안 하고 조용히 리턴하는 코드 경로가 있어서 그게 원인이었을 가능성 추정, 지금은 안 걸림). 대신 pick 도중 **팔(`joint_trajectory_controller`) 이동이 goal tolerance를 못 맞춰서 하강/접근 스텝에서 막히는 걸 새로 확인함** — 아래 항목 참고.
- (미해결) UR5e pick의 팔 이동(하강/접근 스텝)이 목표 joint tolerance에 못 들어가는 경우가 있음(`joint_trajectory_controller`가 결국 `GOAL_TOLERANCE_VIOLATED`로 abort, `FollowJointTrajectory.Result.error_code=-5`). `gz_ros_control`이 팔 관절(`shoulder_pan_joint` 등)에 대해서도 "Command of at least one joint is out of limits" 에러를 반복 출력하는데, 로그의 `actual`과 `command`는 같은 값인데 `limited`만 전혀 다른 값(예: 둘 다 -0.598인데 limited는 -2.197)으로 찍혀서 — 그리퍼 관절에서만 보이던 게 아니라 팔 관절에서도 나는 걸 보면 로봇/조인트별 문제가 아니라 `gz_ros2_control`의 joint-limits enforcement 플러그인 쪽 공통 버그(또는 최소한 오해의 소지가 큰 로그)로 보임 — 실제로 이 경고 때문에 명령이 진짜 클램프되는 건지, 그냥 로그가 잘못 찍히는 것뿐인지 미확인. `/clock` 실측 결과 이 환경의 real_time_factor가 ~0.6(벽시계 85초 동안 sim 51.5초 진행)이라 sim-time 기준 타임아웃 튜닝 시 이것도 감안해야 함(아래 `ur5e_controllers.yaml`의 `goal_time` 참고). 다음에 볼 때는 `gz_ros2_control`의 joint limits enforcement 소스 확인하거나, 이 경고가 실제 동작에 영향 주는지부터 격리 필요.

## 알려진 미해결/진행 중인 것들

- **(2026-09-08) Panda pick이 여전히 안 됨 — 근본 원인 2개 찾아서 고쳤지만 완전 해결은 아직.** DetachableJoint(그리퍼가 닫히면 물체를 강제로 용접)를 한 번 시도했다가 되돌림 — 접촉/마찰을 전혀 시뮬레이션 안 하고 무조건 붙여버려서 "쥐는" 게 아니라 사실상 텔레포트/자석이었고(사용자 피드백으로 확인), vision이 살짝 틀렸을 때 허공에 물체가 붙어 딸려가는 사고까지 냄 — **DetachableJoint는 이 프로젝트에서 쓰지 않기로 함**, 순수 마찰 그립으로 계속 감. 그 과정에서 찾아 고친 실제 버그 2개:
  1. **panda_finger_joint2가 mimic 제약을 못 받고 있었음** — Panda도 UR5e와 똑같이 "물리엔진이 mimic constraint 미지원"(dartsim) 문제를 겪고 있었음(`Physics.cc` 에러 로그로 확인). `panda_gazebo_moveit.launch.py`의 `gz_args`에 `--physics-engine gz-physics-bullet-featherstone-plugin` 추가해서 해결 — 이제 손가락 두 개가 실제로 같이 움직임(이전엔 한쪽만 움직였을 가능성 높음, 그러면 애초에 양쪽에서 못 집으니 마찰이고 뭐고 소용없음).
  2. **그리퍼가 목표 위치(0.014)에 항상 오차 없이 정확히 도달하고 물체에 닿아도 절대 멈칫하지 않는 게 수상해서 파보니, gz_ros2_control이 effort 인터페이스 없는 position-only 관절을 "실제 힘 없이 위치만 세팅"하는 기본 동작이었음(`position_proportional_gain` 파라미터, 기본값 0.1로 사실상 거의 힘을 안 씀). `gazebo_robot/panda_ros2_controllers.yaml`(원본 외부 패키지 yaml을 프로젝트로 복사)에 `gz_ros_control: ros__parameters: position_proportional_gain: 200.0`을 추가하고 launch 파일(`ROS2_CONTROLLERS_YAML`)/xacro(`<parameters>` 경로)가 이 사본을 쓰도록 바꿔서 실제 힘이 나가게 함(**주의**: 관절별이 아니라 이 하드웨어 인스턴스의 position-only 관절 전체—팔 7개+그리퍼—에 공통 적용되는 단일 스칼라값, `ros2 param list /gz_ros_control`로 직접 확인함).
  처음엔 `position_proportional_gain: 200`으로 올렸더니 그리퍼는 힘이 생겼지만 **팔이 심하게 떨렸음**(사용자가 실측 확인, `/joint_states` 속도로도 확인함 — 가만히 있는 home 자세에서도 관절 속도가 최대 6 rad/s, 위치가 0.29rad(~16도)까지 흔들림). 원인: `position_proportional_gain`은 관절별이 아니라 **이 프로세스의 모든 position-only 관절(팔 7개+그리퍼)이 공유하는 단일 스칼라**라(`ros2 param list /gz_ros_control`로 확인, 관절별 맵으로 주면 타입이 안 맞아 조용히 무시됨), 그리퍼에 맞춘 높은 게인이 훨씬 무거운 팔 관절에는 과도해서 진동한 것. **`2.0`으로 낮추니 팔 떨림은 완전히 사라지고(정지 시 관절 속도 ~0.008 이하) 그리퍼도 여전히 충분한 힘을 냄** — `A자재`/1번 팔레트 pick을 재시도 없이 첫 시도에 성공(`material_a` z=0.1826, 안착 0.118 대비 확실히 들림), 컨베이어로 이동/place/home까지 전체 플랜 성공(`{"success":true}`). **DetachableJoint 없이 순수 마찰만으로 pick-place가 실제로 동작하는 것까지 실측 확인 완료** (`gazebo_robot/panda_ros2_controllers.yaml`의 `position_proportional_gain: 2.0`, 2026-09-08). 다만 place 시 물체가 목표 좌표(0.0, 0.55)에서 x로 14cm 정도 벗어난 곳에 놓인 것 확인됨(release 시 관성/구르는 정도로 추정, place에는 아직 vision 보정 없음) — 필요하면 place에도 vision 보정 추가 검토.
- **(2026-09-09) 2번 팔레트 하강 밀림 — `compute_cartesian_path`로 해결 완료.** `taskplan_bridge.py`에 `_compute_cartesian_waypoints()`/`move_cartesian()` 추가 — `/compute_cartesian_path` 서비스(move_group이 MoveItPy 없이도 직접 제공, `compute_ik`와 동일하게 우회 없이 씀)로 현재 자세→목표까지 직선 경로를 여러 waypoint(관절각)로 풀어서 `FollowJointTrajectory`에 다중 포인트로 보낸다. pick의 하강/상승, place의 하강/상승 전부 이걸로 교체. 실측: 2번 팔레트에서 하강+닫기 동안 물체(`material_b`) 위치가 전혀 안 변하는 것 확인(이전엔 밀림). `move_to_joints()`의 `duration_sec` 초 단위 버림 버그(`int(duration_sec)`라 4.5초가 4초가 됨)도 이 김에 나노초까지 정확히 계산하도록 같이 고침. **주의**: 컨베이어 place 시 물체가 목표에서 크게 벗어나는 문제(아래 참고)는 이 밀림 문제와 무관하게 여전히 남아있음 — 하강 자체는 안 미는데 place된 결과 위치가 여전히 부정확함, 원인 미조사.
- 로봇 이동이 전반적으로 느림 — `FollowJointTrajectory`에 고정 시간(3~4.5초)만 주는 방식이라 실제 로봇 속도가 아니라 일부러 눌러놓은 속도임. 개선 옵션 3가지 논의했지만 아직 미결정 (위험 구간만 느리게/전체 시간 단축/MoveIt 실경로계획으로 전환).
- 다른 로봇 모델 추가: UR5e + Robotiq 2F-85 그리퍼, Phase 2a(Gazebo 스폰 + 팔/그리퍼 개폐 검증)까지 완료됨.
  - Phase 1(팔만, 그리퍼 없음): `gazebo_robot/ur5e_gazebo.launch.py`. apt로 설치한 `ros-lyrical-ur-simulation-gz`(+`ur-description`/`ur-moveit-config`/`ur-controllers`) 사용, `FollowJointTrajectory`로 목표 관절각 도달까지 검증.
  - Phase 2a(그리퍼 추가): `gazebo_robot/ur5e_robotiq_gazebo.launch.py` + `ur5e_robotiq_gazebo.urdf.xacro` + `ur5e_gripper.ros2_control.xacro` + `ur5e_controllers.yaml`(신규). apt로 설치한 `ros-lyrical-robotiq-description`의 `ur_to_robotiq_adapter.urdf.xacro`(UR `tool0` → 그리퍼 어댑터)를 사용. 팔(`joint_trajectory_controller`) + 그리퍼(`gripper_controller`, `parallel_gripper_action_controller/GripperActionController`, Panda의 `panda_hand_controller`와 같은 타입이라 `ParallelGripperCommand` 액션 인터페이스도 동일) 둘 다 개폐/이동 검증 완료.
  - 이 배포판 고유 버그 3개 발견/우회 (재발하면 참고):
    1. `ros-lyrical-ur-controllers` 6.0.0의 `controller_plugins.xml`에 `ScaledJointTrajectoryController` 등록이 빠져 있어 표준 `joint_trajectory_controller/JointTrajectoryController`로 대체.
    2. 벤더 launch 파일(`ur_sim_control.launch.py`)을 그대로 쓰면 컨트롤러 노드가 `joints` 파라미터를 못 받아 초기화 실패 → Panda와 동일하게 컨트롤러 spawner에 `-p <yaml경로>` 직접 지정해서 우회.
    3. 기본 물리엔진(dartsim)이 URDF `<mimic>` 조인트의 물리 제약을 지원하지 않아("physics engine does not support mimic constraints") Robotiq 그리퍼(mimic 관절 5개짜리 4절 링크 기구)가 자기 링크끼리 간섭하며 안 움직임 → `gz sim` 실행 시 `--physics-engine gz-physics-bullet-featherstone-plugin`으로 물리엔진 교체해서 해결(공유 `pallets_world.sdf` 파일 자체는 안 건드림, launch 파일의 `gz_args`에서만 오버라이드 — Panda 쪽엔 영향 없음).
  - Task Plan 파이프라인 연동(Phase 2b): `taskplan_bridge.py` 로봇별 어댑터 리팩터링, `api_server.py` 로봇 선택(`FORSTICK_ROBOT` 환경변수 + `/health` 노출 + 콘솔 UI 반영), `LOCATION_POSES` UR5e용 재계산 — 전부 완료. 실제 Task Plan으로 팔 이동(IK+`FollowJointTrajectory`)까지는 매번 성공 확인함. `taskplan_bridge.py`의 `move_to_joints`/`set_gripper`에 원래 타임아웃이 없어서(있던 건 `compute_ik`뿐) 응답 없는 액션이 무한 대기로 이어질 수 있었는데, 지금은 둘 다 5~15초 타임아웃+재시도를 추가해서 최소한 깔끔하게 실패하도록 고쳐둠. (2026-09-04) 예전에 "그리퍼가 막혀서 pick이 안 된다"고 기록했던 것 재확인해보니 그리퍼 자체는 재현 안 됐고, 대신 `ur5e_controllers.yaml`의 `joint_trajectory_controller`가 `goal_time: 0.0`(무제한)으로 설정돼 있어서 팔 이동이 물리적으로 도달 불가능한 상황이면 서버가 영원히 "실행 중" 상태로 안 끝나는 버그를 발견/수정함(`goal_time: 5.0`으로 복구 — 이 환경 real_time_factor ~0.6 감안한 값, 위 "이 배포판에서 만난 버전 고유 버그들" 참고). 이걸 고치고 나니 pick의 하강/접근 스텝이 실제로는 `GOAL_TOLERANCE_VIOLATED`로 계속 실패하고 있다는 게 (타임아웃에 가려져있다가) 드러남 — 아직 미해결, pick은 여전히 안 됨. 추가로 `taskplan_bridge.py`에 `/clock` 구독 기반 `get_real_time_factor()`를 넣어서 `move_to_joints`/`set_gripper`의 응답 대기 타임아웃을 실측 RTF로 스케일링하도록 고침(`_rtf_scaled_timeout`) — 이 환경 RTF가 실행마다 0.3~0.8 정도로 크게 들쭉날쭉해서, 고정 타임아웃(예: duration_sec+15초, RTF=1.0 가정)으로는 서버가 정상적으로 abort하기도 전에 클라이언트가 먼저 포기해버리는 경우가 실측으로 확인됐음(goal_time 자체는 지켜졌는데도 벽시계 타임아웃엔 못 미침). **단, 이 RTF 추정은 `enable_background_spin()`을 쓰는 경로(api_server.py, 실제 운영 경로)에서만 정확하게 동작함을 실측 확인** — `taskplan_bridge.py`를 독립 스크립트(CLI)로 돌리면 명시적으로 기다릴 때만 짧게 spin해서 `/clock` 메시지가 몰려서 도착하고, `wall_elapsed` 표본 구간이 실제 경과 벽시계 시간을 반영 못 해 default(RTF=1.0)로 자주 떨어짐 — CLI 모드는 기존과 동일하게 다소 보수적으로 동작(회귀는 아님, 그냥 최적화가 안 먹힐 뿐). api_server.py 경로로 실측한 RTF는 세션마다 0.3~0.85 정도로 관찰됨.
- (2026-09-04 테스트함) 팔레트 1번 그래스핑: `/v1/execute`로 실제 pick 실행해봄 — 액션 자체는 전부 성공 응답(`{"success":true}`, 그리퍼 열기/접근/하강/닫기/복귀 전부 error_code=0)했지만, `gz topic -e -t /world/pallets_world/pose/info`로 `material_a`의 실제 위치를 직접 확인해보니 팔이 hover 높이(z=0.3)로 복귀한 뒤에도 물체는 원래 놓여있던 grasp 높이(z≈0.1175)에 그대로 남아있었음 — 즉 **한 번도 들어올려지지 않음**. 손가락은 정확히 0.014(의도한 그립 폭)까지 닫혔는데도 마찰만으로는 안 들림. 3번 팔레트에서만 확인됐던 "그리퍼가 마찰력만으로 붙잡아서 놓침" 문제(아래 참고)가 1번 팔레트에서도 동일하게 재현됨 — Task Plan API의 `{"success":true}` 응답만으로는 실제 grasp 성공을 보장 못 한다는 뜻이라, 앞으로 pick 검증할 땐 액션 성공 여부 말고 물체 위치를 직접 확인해야 함.
  - DetachableJoint 근본 해결 관련 진전: 그동안 막혀있던 "정확한 Gazebo Jetty 문법 미확인"(`gz sim --versions` 필요) 블로커를 셸에서 직접 확인해서 풀었음 — 이 환경 gz-sim 버전은 **10.5.0**, 정확한 플러그인 문법 예제가 `/opt/ros/lyrical/opt/gz_sim_vendor/share/gz/gz-sim/worlds/detachable_joint.sdf`에 이미 설치돼 있음(패키지: `libgz-sim-detachable-joint-system.so`). 로봇(부모) 쪽 URDF/xacro에 `<gazebo><plugin filename="gz-sim-detachable-joint-system" name="gz::sim::systems::DetachableJoint"><parent_link>...</parent_link><child_model>material_a</child_model><child_link>...</child_link><detach_topic>/material_a/detach</detach_topic><attach_topic>/material_a/attach</attach_topic></plugin></gazebo>` 형태로 물체별(A/B/C)로 3개 선언해두고, `taskplan_bridge.py`의 pick/place에서 그리퍼 닫을 때 attach, 열 때 detach 토픽을 쏘는 방식으로 구현 가능 — 아직 미착수(다음 작업 후보).
- **(2026-09-09) 그리퍼 힘 부족 문제 — 여러 방법 시도, 전부 실패/원복. 아직 미해결.** 근본 원인은 여전히 `position_proportional_gain`이 팔 7개+그리퍼가 공유하는 단일 스칼라라서(2026-09-08 항목 참고), 팔이 안 떨리는 낮은 값(2.0)에서는 그리퍼가 물체 접촉 저항을 확실하게 못 이긴다는 것. 오늘 시도한 것들:
  1. **그리퍼 close 명령을 여러 단계로 나눠 보내기** (`set_gripper`가 매번 `_get_gripper_position()`으로 실측 현재값을 읽어 max_step 이내로 여러 번 나눠 전송) — 큰 오차를 한 번에 명령하면 gz_ros2_control이 속도 한도 위반으로 명령 자체를 뭉개는 버그(팔에서 봤던 것과 같은 계열, "Command of at least one joint is out of limits")는 피했지만, 힘의 절대 상한 자체가 게인에 묶여있어서 grip 성공률은 안 나아짐 — **원복함**.
  2. **`gz_ros2_control` 플러그인을 그리퍼 전용으로 하나 더 인스턴스화**(`panda_gazebo.urdf.xacro`에 두 번째 `<gazebo><plugin>`, `controller_manager_name`으로 별도 이름) — 그리퍼만 다른 게인을 쓰게 하려 했으나, 실측해보니 **두 번째 인스턴스도 로봇 전체(팔+그리퍼) 하드웨어를 다시 활성화**해버림("Activating component 'PandaGazeboSystem'"이 두 controller_manager 로그에 모두 찍힘) — 컨트롤러 없는 쪽 인스턴스가 매 스텝 기본값(0)을 써서 실제 컨트롤러의 명령을 덮어써버려 그리퍼가 아예 안 움직이는 상태가 됨. 이 gz_ros2_control 버전엔 플러그인 인스턴스가 관리할 `<ros2_control>` 리소스를 이름으로 제한하는 방법이 안 보임 — **원복함**.
  3. **그리퍼를 position 대신 effort 커맨드 인터페이스로 전환**(`panda_hand.ros2_control.xacro`, `panda_hand_controller`를 `forward_command_controller/ForwardCommandController` + `interface_name: effort`로) — 이론적으로는 `position_proportional_gain`을 완전히 우회해서 팔 게인과 무관하게 힘을 낼 수 있는 올바른 방향이었지만:
     - 처음엔 effort state_interface를 안 선언해서 `JointFeatures.cc`가 "Invalid joint force value [nan]" 반복 (gz_ros2_control_demos의 effort 예제처럼 `<state_interface name="effort"/>`도 같이 선언하니 해결).
     - **dartsim**: 힘은 정상적으로 걸려서 관절이 자유롭게 움직임(9N으로 0↔0.04 왕복 확인)이지만, **실제 물체 접촉을 전혀 못 잡고 그냥 통과해버림** — `/scene_camera` 프레임을 직접 캡처해서 확인함(닫은 직후 프레임에 물체가 그대로 팔레트에 남아있고, 상승 후에도 그리퍼는 빈손). 속도를 `max_speed`로 제한하는 폐루프(bang-bang)까지 시도했지만 여전히 못 잡음 — 속도(터널링) 문제가 아니라 접촉 자체가 감지가 안 되는 것으로 보임.
     - **bullet-featherstone**(원래 쓰던 물리엔진, mimic 지원 때문에 필요): 반대로 완전열림(0.04)에서 닫는 방향으로 -4N/-9N 다 줘도 전혀 안 움직이는 경우가 실측됨 — 물리엔진마다 완전히 다른 방식으로 고장남.
     - 이 시도 도중 실수로 원래 되던 pallet 1 pick까지 한동안 실패 상태로 만들었음(사용자 피드백으로 인지) — **전부 원복**: `panda_hand.ros2_control.xacro`/`panda_ros2_controllers.yaml`/`robot_config.py`/`taskplan_bridge.py`의 `set_gripper()` 모두 2026-09-08 상태(position 인터페이스, 공유 gain=2.0, 단일 명령)로 되돌렸고, 원복 후 pallet 1(`material_a`) pick→move→place→home 재시도 없이 성공 재확인함(`compute_cartesian_path` 하강/상승 fix는 그대로 유지된 채로).
  - **다음에 시도해볼 만한 것** (아직 미착수): effort 인터페이스 자체는 방향이 맞아 보이므로, dartsim에서 접촉이 안 잡히는 이유(충돌 마진/타임스텝?)를 더 깊이 파거나, bullet-featherstone에서 왜 닫는 방향으로만 안 움직이는지(조인트 상한 근처 stiction?) 조사해볼 가치가 있음 — 단, 하루 이상 걸릴 수 있는 물리엔진 저수준 디버깅이라 신중하게 시간 배분할 것.
- 감사 로그(`audit_log.jsonl`)는 로컬 파일만, DB 연동 안 됨.
- "저거 옮겨줘" 같은 모호한 지시(대명사 참조)는 미지원 — 대화 맥락 추적이나 비전 그라운딩 필요, 범위 밖으로 보류.

## 팀 역할 분담 (참고용)

- 김예은(팀장): 프로젝트 총괄, DB 설계, L3 Safety Guard
- 김남수(나): 백엔드 API 서버, 형식검증, 감사 로그, 통합 — 지금까지 혼자 풀스택으로 진행 중 (팀 경계 무시하고 진행하기로 함)
- 손대호: PWA, ROS2/MoveIt2 미들웨어 통합, Gazebo, L2(LLM 프롬프트/스킬카탈로그)
- 이윤예: L1(STT+슬롯추출), "정지" 고속경로, 평가 데이터셋

## 컨벤션

- 파일 전체를 다시 쓸 때는 diff보다 전체 내용으로 — 이건 Cowork에서 heredoc 복붙 방식일 때 필요했던 규칙이라, Claude Code에서 직접 파일 편집할 땐 그냥 일반적인 Edit 방식 써도 됨.
- `pipeline.py`/`taskplan_bridge.py`/`api_server.py` 수정 후에는 uvicorn 재시작 필요 (`--reload` 안 씀). `demo.html`은 매 요청마다 새로 읽으므로 재시작 불필요. `*.sdf` 월드 파일은 Gazebo 재실행 필요.
