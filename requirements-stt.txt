# 실시간 STT 실제 모델 의존성 (md/STT_모델_구성.md 참고)
#
# 고정 이유: 같은 구성을 다시 만들 수 있어야 측정 결과를 비교할 수 있다.
# 설치 전 `pip install --dry-run`으로 기존 패키지와의 충돌을 확인했다.
#
# torch를 쓰지 않는다 — faster-whisper 1.2.1이 Silero VAD ONNX 모델을 번들하고
# onnxruntime으로 실행하므로 torch(약 2.5GB)가 필요 없다.
#
# numpy·pydantic·jsonschema는 시스템 패키지를 재사용한다(venv --system-site-packages).
faster-whisper==1.2.1
ctranslate2==4.8.1
onnxruntime==1.29.0
